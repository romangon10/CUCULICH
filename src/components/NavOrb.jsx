import { useEffect,useRef,useState } from 'react';
import { Menu,X } from 'lucide-react';
const links=[['Colección','#coleccion'],['Manifiesto','#manifiesto'],['Atelier','#atelier'],['Contacto','#contacto']];
export default function NavOrb(){const [open,setOpen]=useState(false);const dialog=useRef(null);const trigger=useRef(null);useEffect(()=>{if(!open)return;const d=dialog.current;const previous=document.body.style.overflow;d.showModal();document.body.style.overflow='hidden';return()=>{d.close();document.body.style.overflow=previous;trigger.current?.focus()};},[open]);return <><button ref={trigger} className="nav-orb glass-obsidian" aria-label="Abrir menú" aria-expanded={open} aria-controls="main-menu" onClick={()=>setOpen(true)}><Menu size={24}/></button><dialog ref={dialog} id="main-menu" className="menu-dialog" onCancel={()=>setOpen(false)} aria-label="Navegación principal"><button className="close-menu" aria-label="Cerrar menú" onClick={()=>setOpen(false)}><X size={30}/></button><nav>{links.map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)}>{label}</a>)}</nav><p className="eyebrow ghost">CUCULICH · MMXXVI</p></dialog></>}
